# Variables de entorno para API de Líneas de Captura

# Puerto del servidor
PORT=3001

# Supabase (usar el mismo proyecto o uno diferente)
SUPABASE_URL=tu_supabase_url
SUPABASE_SERVICE_KEY=tu_service_key

# Nota: Puedes usar el mismo proyecto de Supabase que usas para multas
# Solo necesitas agregar la tabla lineas_captura con el schema.sql
